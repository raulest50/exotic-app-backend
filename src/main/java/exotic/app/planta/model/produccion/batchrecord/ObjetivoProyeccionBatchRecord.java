package exotic.app.planta.model.produccion.batchrecord;

/** Último estado documental solicitado por la operación fuente. */
public enum ObjetivoProyeccionBatchRecord {
    ACTIVO,
    CERRADO,
    ANULADO
}
